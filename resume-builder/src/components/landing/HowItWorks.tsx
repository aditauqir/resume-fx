import { TextReveal } from "@/components/ui/text-reveal";

export function HowItWorks() {
  return (
    <section>
      <TextReveal>
        Upload your resume. Pick your AI model. Start applying to jobs faster than ever.
      </TextReveal>
    </section>
  );
}

